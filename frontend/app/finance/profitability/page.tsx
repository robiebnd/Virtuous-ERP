"use client";
import {useEffect,useState} from "react";
import {financeApi} from "@/lib/api";

export default function ProfitabilityPage(){
 const [companies,setCompanies]=useState<any[]>([]),[segments,setSegments]=useState<any[]>([]),[report,setReport]=useState<any[]>([]),[rules,setRules]=useState<any[]>([]),[allocationRuns,setAllocationRuns]=useState<any[]>([]),[error,setError]=useState("");
 const [company,setCompany]=useState("ZW01"),[year,setYear]=useState(new Date().getFullYear()),[period,setPeriod]=useState(1);
 const [form,setForm]=useState({segmentCode:"",segmentName:"",companyCode:"ZW01",productCode:"",salesChannel:"",marketRegion:"",customerGroup:"",productGroup:""});\n const [postingForm,setPostingForm]=useState({segmentId:"",accountCode:"400000",amount:"",revenue:true,currency:"USD",postingDate:new Date().toISOString().slice(0,10),quantity:""});
 const [allocationForm,setAllocationForm]=useState({ruleCode:"",ruleName:"",sourceSegmentId:"",driverType:"PERCENTAGE"});
 const [targetForm,setTargetForm]=useState({ruleId:"",targetSegmentId:"",allocationPercent:"100"});
 const load=async(c=company)=>{try{const [co,s]=await Promise.all([financeApi.companyCodes(),financeApi.profitabilitySegments(c)]);setCompanies(co);setSegments(s);setRules(await financeApi.copaAllocationRules(c));setAllocationRuns(await financeApi.copaAllocationRuns(c));setForm(x=>({...x,companyCode:c}));setError("")}catch(e){setError(e instanceof Error?e.message:"Unable to load CO-PA.")}};
 useEffect(()=>{load()},[]);
 const create=async()=>{try{await financeApi.createProfitabilitySegment(form);await load(company)}catch(e){setError(e instanceof Error?e.message:"Unable to create segment.")}};\n const post=async()=>{try{if(!postingForm.segmentId)throw new Error("Select a profitability segment.");await financeApi.postProfitability({...postingForm,companyCode:company,amount:Number(postingForm.amount),quantity:Number(postingForm.quantity||0)});await run();setError("")}catch(e){setError(e instanceof Error?e.message:"Unable to post profitability entry.")}};
 const createRule=async()=>{try{const r=await financeApi.createCopaAllocationRule({...allocationForm,companyCode:company});setRules(await financeApi.copaAllocationRules(company));setTargetForm(x=>({...x,ruleId:r.id}));setError("")}catch(e){setError(e instanceof Error?e.message:"Unable to create allocation rule.")}};
 const addTarget=async()=>{try{await financeApi.addCopaAllocationTarget({...targetForm,allocationPercent:Number(targetForm.allocationPercent)});setError("")}catch(e){setError(e instanceof Error?e.message:"Unable to add allocation target.")}};
 const runAllocation=async()=>{try{if(!targetForm.ruleId)throw new Error("Select an allocation rule.");await financeApi.runCopaAllocation(targetForm.ruleId,{fiscalYear:year,periodNumber:period,runBy:"CURRENT_USER"});setAllocationRuns(await financeApi.copaAllocationRuns(company));setError("")}catch(e){setError(e instanceof Error?e.message:"Unable to run CO-PA allocation.")}};
 const run=async()=>{try{setReport(await financeApi.profitabilityReport(company,year,period));setError("")}catch(e){setError(e instanceof Error?e.message:"Unable to load profitability report.")}};
 return <main className="content">
  <div className="page-head"><div><div className="eyebrow">FINANCE / CO-PA</div><h1>Profitability Analysis</h1><p>Account-based profitability by product, customer, channel and market dimensions, reconciled from accounting postings.</p></div><div className="actions"><button className="btn primary" onClick={()=>run()}>Refresh Report</button></div></div>
  {error&&<div className="alert error">{error}</div>}
  <div className="card" style={{marginBottom:18}}><div className="toolbar"><div className="filter-field"><label>Company Code</label><select className="filter" value={company} onChange={e=>{setCompany(e.target.value);load(e.target.value)}}>{companies.map(x=><option key={x.id}>{x.companyCode}</option>)}</select></div><div className="filter-field"><label>Fiscal Year</label><input className="filter" type="number" value={year} onChange={e=>setYear(Number(e.target.value))}/></div><div className="filter-field"><label>Period</label><select className="filter" value={period} onChange={e=>setPeriod(Number(e.target.value))}>{Array.from({length:12},(_,i)=><option key={i+1}>{i+1}</option>)}</select></div></div></div>
  <div className="grid" style={{gridTemplateColumns:"1fr 2fr",marginBottom:18}}>
   <section className="card form-card"><div className="section-title">Profitability Segment</div><div className="form-grid" style={{gridTemplateColumns:"1fr"}}>
    {Object.entries(form).filter(([k])=>k!=="companyCode").map(([k,v])=><div className="form-field" key={k}><label>{k}</label><input className="form-input" value={v as string} onChange={e=>setForm({...form,[k]:e.target.value})}/></div>)}<button className="btn primary" onClick={create}>Create Segment</button>
   </div></section>
   <section className="card"><div className="table-caption"><strong>Configured Segments</strong><span>{segments.length} segments</span></div><div className="table-wrap"><table className="table"><thead><tr><th>Code</th><th>Name</th><th>Product</th><th>Channel</th><th>Region</th><th>Customer Group</th></tr></thead><tbody>{segments.map(x=><tr key={x.id}><td>{x.segmentCode}</td><td>{x.segmentName}</td><td>{x.productCode||"—"}</td><td>{x.salesChannel||"—"}</td><td>{x.marketRegion||"—"}</td><td>{x.customerGroup||"—"}</td></tr>)}</tbody></table></div></section>
  </div>
  <section className="card" style={{marginBottom:18}}><div className="section-title">Integrated CO-PA Posting</div><p className="muted" style={{padding:"0 22px"}}>Post a revenue or cost line directly to the general ledger with a validated profitability segment.</p><div className="form-grid"><div className="form-field"><label>Profitability Segment</label><select className="form-input" value={postingForm.segmentId} onChange={e=>setPostingForm({...postingForm,segmentId:e.target.value})}><option value="">Select segment</option>{segments.map(x=><option key={x.id} value={x.id}>{x.segmentCode} — {x.segmentName}</option>)}</select></div><div className="form-field"><label>GL Account</label><input className="form-input" value={postingForm.accountCode} onChange={e=>setPostingForm({...postingForm,accountCode:e.target.value})}/></div><div className="form-field"><label>Amount</label><input className="form-input" type="number" min="0.01" step="0.01" value={postingForm.amount} onChange={e=>setPostingForm({...postingForm,amount:e.target.value})}/></div><div className="form-field"><label>Type</label><select className="form-input" value={postingForm.revenue?"REVENUE":"COST"} onChange={e=>setPostingForm({...postingForm,revenue:e.target.value==="REVENUE"})}><option value="REVENUE">REVENUE</option><option value="COST">COST</option></select></div><div className="form-field"><label>Currency</label><input className="form-input" value={postingForm.currency} onChange={e=>setPostingForm({...postingForm,currency:e.target.value})}/></div><div className="form-field"><label>Posting Date</label><input className="form-input" type="date" value={postingForm.postingDate} onChange={e=>setPostingForm({...postingForm,postingDate:e.target.value})}/></div><div className="form-field"><label>Quantity</label><input className="form-input" type="number" min="0" step="0.001" value={postingForm.quantity} onChange={e=>setPostingForm({...postingForm,quantity:e.target.value})}/></div><div className="form-field"><label>&nbsp;</label><button className="btn primary" onClick={post}>Post to GL and CO-PA</button></div></div></section>\n  <section className="card" style={{marginBottom:18}}><div className="section-title">CO-PA Allocations</div><p className="muted" style={{padding:"0 22px"}}>Define allocation rules for shared costs and run controlled period allocations across profitability segments.</p><div className="form-grid"><div className="form-field"><label>Rule Code</label><input className="form-input" value={allocationForm.ruleCode} onChange={e=>setAllocationForm({...allocationForm,ruleCode:e.target.value})}/></div><div className="form-field"><label>Rule Name</label><input className="form-input" value={allocationForm.ruleName} onChange={e=>setAllocationForm({...allocationForm,ruleName:e.target.value})}/></div><div className="form-field"><label>Source Segment</label><select className="form-input" value={allocationForm.sourceSegmentId} onChange={e=>setAllocationForm({...allocationForm,sourceSegmentId:e.target.value})}><option value="">Select source</option>{segments.map(x=><option key={x.id} value={x.id}>{x.segmentCode} — {x.segmentName}</option>)}</select></div><div className="form-field"><label>Driver</label><select className="form-input" value={allocationForm.driverType} onChange={e=>setAllocationForm({...allocationForm,driverType:e.target.value})}><option>PERCENTAGE</option><option>HEADCOUNT</option><option>REVENUE</option><option>VOLUME</option></select></div><div className="form-field"><label>&nbsp;</label><button className="btn" onClick={createRule}>Create Rule</button></div></div><div className="form-grid"><div className="form-field"><label>Rule</label><select className="form-input" value={targetForm.ruleId} onChange={e=>setTargetForm({...targetForm,ruleId:e.target.value})}><option value="">Select rule</option>{rules.map(x=><option key={x.id} value={x.id}>{x.ruleCode} — {x.ruleName}</option>)}</select></div><div className="form-field"><label>Target Segment</label><select className="form-input" value={targetForm.targetSegmentId} onChange={e=>setTargetForm({...targetForm,targetSegmentId:e.target.value})}><option value="">Select target</option>{segments.map(x=><option key={x.id} value={x.id}>{x.segmentCode} — {x.segmentName}</option>)}</select></div><div className="form-field"><label>Allocation %</label><input className="form-input" type="number" min="0.01" max="100" step="0.01" value={targetForm.allocationPercent} onChange={e=>setTargetForm({...targetForm,allocationPercent:e.target.value})}/></div><div className="form-field"><label>&nbsp;</label><button className="btn" onClick={addTarget}>Add Target</button></div><div className="form-field"><label>&nbsp;</label><button className="btn primary" onClick={runAllocation}>Run Allocation</button></div></div><div className="table-wrap"><table className="table"><thead><tr><th>Rule</th><th>Year</th><th>Period</th><th>Allocated Amount</th><th>Status</th><th>Run By</th></tr></thead><tbody>{allocationRuns.map(x=><tr key={x.id}><td>{x.rule?.ruleCode||"—"}</td><td>{x.fiscalYear}</td><td>{x.periodNumber}</td><td>{Number(x.allocatedAmount||0).toFixed(2)}</td><td>{x.status}</td><td>{x.runBy}</td></tr>)}</tbody></table></div></section>
  <section className="card"><div className="table-caption"><strong>Contribution Margin Report</strong><span>{company} · FY {year} / P{period}</span></div><div className="table-wrap"><table className="table"><thead><tr><th>Segment</th><th>Product</th><th>Channel</th><th>Region</th><th>Customer Group</th><th>Revenue</th><th>Cost</th><th>Contribution Margin</th><th>Margin %</th></tr></thead><tbody>{report.map(x=>{const margin=x.revenue?Number(x.contributionMargin)/Number(x.revenue)*100:0;return <tr key={x.segmentId}><td><strong>{x.segmentCode}</strong> — {x.segmentName}</td><td>{x.productCode||"—"}</td><td>{x.salesChannel||"—"}</td><td>{x.marketRegion||"—"}</td><td>{x.customerGroup||"—"}</td><td>{Number(x.revenue||0).toFixed(2)}</td><td>{Number(x.cost||0).toFixed(2)}</td><td>{Number(x.contributionMargin||0).toFixed(2)}</td><td>{margin.toFixed(1)}%</td></tr>})}</tbody></table></div></section>
 </main>
}