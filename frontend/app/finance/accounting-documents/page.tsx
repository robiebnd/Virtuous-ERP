"use client";
import {useEffect,useState} from "react";
import {financeApi} from "@/lib/api";
import {ModuleWorkspace} from "@/components/ModuleWorkspace";
export default function AccountingDocumentsPage(){const[rows,setRows]=useState<any[]>([]);const[error,setError]=useState("");useEffect(()=>{financeApi.accountingDocuments().then(setRows).catch(e=>setError(e.message||"Unable to load accounting documents."));},[]);return <main className="content"><div className="page-head"><div><div className="eyebrow">FINANCE / FI-DOCUMENTS</div><h1>Accounting Documents</h1><p>Review posted financial accounting documents and their posting information.</p></div><button className="btn" onClick={()=>financeApi.accountingDocuments().then(setRows)}>Refresh</button></div>{error&&<div className="alert error">{error}</div>}<ModuleWorkspace title="Posted Accounting Documents" subtitle={`${rows.length} accounting documents`} searchPlaceholder="Search document, reference or type..." columns={[{key:"documentNumber",label:"Document"},{key:"documentType",label:"Type"},{key:"referenceNumber",label:"Reference"},{key:"postingDate",label:"Posting Date"},{key:"currency",label:"Currency"},{key:"totalDebit",label:"Debit"},{key:"totalCredit",label:"Credit"},{key:"status",label:"Status"}]} rows={rows}/></main>}