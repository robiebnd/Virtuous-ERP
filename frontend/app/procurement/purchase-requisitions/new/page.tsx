"use client";
import {useState} from "react";
import {useRouter} from "next/navigation";
import {procurementApi} from "@/lib/api";
import {ReferenceSelect} from "@/components/ReferenceSelect";
export default function NewPurchaseRequisition(){const router=useRouter();const[busy,setBusy]=useState(false);const[error,setError]=useState("");const[f,setF]=useState({warehouseId:"",supplierId:"",department:"PROCUREMENT",currency:"USD",documentType:"NB",purchasingGroup:"PURCHASING",plantCode:"MAIN",storageLocation:"MAIN",itemCategory:"STANDARD",accountAssignmentCategory:"K",requestedDeliveryDate:"",valuationPrice:""});const set=(k:string,v:string)=>setF(x=>({...x,[k]:v}));const save=async(submit=false)=>{setBusy(true);setError("");try{const pr=await procurementApi.createRequisition({...f,valuationPrice:f.valuationPrice?Number(f.valuationPrice):undefined,requestedDeliveryDate:f.requestedDeliveryDate?`${f.requestedDeliveryDate}T00:00:00`:undefined});if(submit)await procurementApi.submitRequisition(pr.id);router.push(`/procurement/purchase-requisitions/${pr.id}`)}catch(e:any){setError(e.message||"Unable to create requisition") }finally{setBusy(false)}};const input=(label:string,key:string,placeholder="")=><div className="form-field"><label>{label}</label><input className="form-input" value={(f as any)[key]} placeholder={placeholder} onChange={e=>set(key,e.target.value)}/></div>;return <div className="content"><div className="page-head"><div><div className="eyebrow">PURCHASE REQUISITION</div><h1>New Purchase Requisition</h1><p>Create a procurement requirement using the SAP-aligned document structure.</p></div><div className="actions"><button className="btn" onClick={()=>router.back()}>Cancel</button><button className="btn" disabled={busy} onClick={()=>save(false)}>Save</button><button className="btn mustard" disabled={busy} onClick={()=>save(true)}>Save & Submit</button></div></div>{error&&<div className="alert error">{error}</div>}<section className="card form-card"><div className="section-title">General Information</div><div className="form-grid"><div className="form-field"><label>Warehouse *</label><ReferenceSelect type="warehouse" value={f.warehouseId} onChange={v=>set("warehouseId",v)} required placeholder="Select warehouse"/></div><div className="form-field"><label>Supplier *</label><ReferenceSelect type="supplier" value={f.supplierId} onChange={v=>set("supplierId",v)} required placeholder="Select supplier"/></div>{input("Department","department")}{input("Currency","currency")}{input("Purchasing Group","purchasingGroup")}{input("Plant","plantCode")}{input("Storage Location","storageLocation")}{input("Purchasing Document Type","documentType")}{input("Item Category","itemCategory")}{input("Account Assignment Category","accountAssignmentCategory")}{input("Requested Delivery Date","requestedDeliveryDate")}{input("Valuation Price","valuationPrice","0.00")}</div></section><section className="card form-card"><div className="section-title">Account Assignment</div><div className="info-grid"><div><span>Category</span><strong>{f.accountAssignmentCategory||"—"}</strong></div><div><span>Cost Center / G/L</span><strong>To be assigned on item</strong></div><div><span>Plant</span><strong>{f.plantCode||"—"}</strong></div></div></section><section className="card form-card"><div className="section-title">Process</div><div className="process"><div className="process-step active"><b>1</b><span>Purchase Requisition</span><small>Draft</small></div><div className="process-line"/><div className="process-step"><b>2</b><span>Approval</span><small>Pending</small></div><div className="process-line"/><div className="process-step"><b>3</b><span>Purchase Order</span><small>Created from approved PR</small></div><div className="process-line"/><div className="process-step"><b>4</b><span>Goods Receipt</span><small>Load PO lines</small></div></div></section></div>}
