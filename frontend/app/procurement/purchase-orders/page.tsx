"use client";
import { useEffect,useState } from "react";
import { procurementApi } from "@/lib/api";
const demo=[{id:"1",purchaseOrderNumber:"PO-000087",supplier:"Supplier A",plant:"MAIN",currency:"USD",total:2500,status:"READY"},{id:"2",purchaseOrderNumber:"PO-000086",supplier:"Supplier B",plant:"MAIN",currency:"USD",total:840,status:"APPROVED"},{id:"3",purchaseOrderNumber:"PO-000085",supplier:"Supplier C",plant:"S001",currency:"USD",total:1250,status:"DRAFT"}];
export default function PurchaseOrders(){const [rows,setRows]=useState<any[]>(demo);useEffect(()=>{procurementApi.orders().then(setRows).catch(()=>{})},[]);return <div className="content"><div className="page-head"><div><h1>Purchase Orders</h1><p>Manage supplier orders created directly or from approved purchase requisitions.</p></div><div className="actions"><button className="btn">Refresh</button><button className="btn primary">New Purchase Order</button></div></div><section className="card"><div className="toolbar"><input className="filter" style={{flex:1,minWidth:240}} placeholder="Search purchase order or supplier..."/><select className="filter"><option>All statuses</option><option>Draft</option><option>Approved</option><option>Ready</option></select></div><div className="table-wrap"><table className="table"><thead><tr><th>Purchase Order</th><th>Supplier</th><th>Plant</th><th>Currency</th><th>Total</th><th>Status</th><th>Next Step</th></tr></thead><tbody>{rows.map(r=><tr key={r.id||r.purchaseOrderNumber}><td><a className="link">{r.purchaseOrderNumber||r.poNumber||r.number||r.id}</a></td><td>{r.supplier?.name||r.supplierName||r.supplier||"—"}</td><td>{r.plantCode||r.plant||"—"}</td><td>{r.currency||"USD"}</td><td>{Number(r.total||r.totalValue||0).toFixed(2)}</td><td><span className={`status ${(r.status||"").toLowerCase().includes("approved")?"approved":(r.status||"").toLowerCase().includes("ready")?"ready":"draft"}`}>{String(r.status||"DRAFT").replaceAll("_"," ")}</span></td><td>{String(r.status||"").includes("READY")?"Goods Receipt":"Approval"}</td></tr>)}</tbody></table></div></section></div>}
