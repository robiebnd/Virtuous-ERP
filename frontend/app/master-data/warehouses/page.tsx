"use client";
import {useEffect,useState} from "react";
import {masterDataApi} from "@/lib/api";
export default function WarehousesPage(){const[rows,setRows]=useState<any[]>([]);const[error,setError]=useState("");useEffect(()=>{masterDataApi.warehouses().then(setRows).catch(e=>setError(e.message||"Unable to load warehouses."));},[]);return <div className="content"><div className="page-head"><div><div className="eyebrow">MASTER DATA / LOGISTICS</div><h1>Warehouses</h1><p>Warehouse master data used by procurement, inventory valuation and outbound operations.</p></div><button className="btn" onClick={()=>masterDataApi.warehouses().then(setRows)}>Refresh</button></div>{error&&<div className="alert error">{error}</div>}<section className="card"><div className="table-caption"><strong>Warehouses ({rows.length})</strong><span>Business-facing warehouse master</span></div><div className="table-wrap"><table className="table"><thead><tr><th>Warehouse Code</th><th>Name</th><th>City</th><th>Country</th><th>Status</th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td>{r.code||"—"}</td><td>{r.name||"—"}</td><td>{r.city||"—"}</td><td>{r.country||"—"}</td><td><span className="status approved">{r.active===false?"Inactive":"Active"}</span></td></tr>)}</tbody></table></div></section></div>}