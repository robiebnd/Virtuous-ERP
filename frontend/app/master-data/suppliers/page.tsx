"use client";
import {useEffect,useState} from "react";
import {masterDataApi} from "@/lib/api";
export default function SuppliersPage(){const[rows,setRows]=useState<any[]>([]);const[error,setError]=useState("");useEffect(()=>{masterDataApi.suppliers().then(setRows).catch(e=>setError(e.message||"Unable to load suppliers."));},[]);return <div className="content"><div className="page-head"><div><div className="eyebrow">MASTER DATA / BUSINESS PARTNERS</div><h1>Suppliers</h1><p>Supplier master data used across purchasing, accounts payable and vendor evaluation.</p></div><button className="btn" onClick={()=>masterDataApi.suppliers().then(setRows)}>Refresh</button></div>{error&&<div className="alert error">{error}</div>}<section className="card"><div className="table-caption"><strong>Active Suppliers ({rows.length})</strong><span>Business-facing supplier master</span></div><div className="table-wrap"><table className="table"><thead><tr><th>Supplier Code</th><th>Name</th><th>Contact</th><th>City</th><th>Country</th><th>Status</th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td>{r.code||"—"}</td><td>{r.name||"—"}</td><td>{r.email||r.phone||"—"}</td><td>{r.city||"—"}</td><td>{r.country||"—"}</td><td><span className="status approved">Active</span></td></tr>)}</tbody></table></div></section></div>}