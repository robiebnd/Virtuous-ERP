package com.digipals.wms.finance.entity; import jakarta.persistence.*; import lombok.*; import java.math.BigDecimal; import java.util.UUID;
@Entity @Table(name="treasury_risk_limits") @Getter @Setter @NoArgsConstructor @AllArgsConstructor @Builder public class TreasuryRiskLimit { @Id @GeneratedValue(strategy=GenerationType.UUID) UUID id; String limitCode; String counterparty; String currency; String instrumentType; BigDecimal limitAmount; boolean active; }