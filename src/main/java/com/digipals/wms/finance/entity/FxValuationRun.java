package com.digipals.wms.finance.entity; import jakarta.persistence.*; import lombok.*; import java.math.BigDecimal; import java.time.LocalDate; import java.util.UUID;
@Entity @Table(name="fx_valuation_runs",uniqueConstraints=@UniqueConstraint(columnNames={"reference_number","valuation_date"})) @Getter @Setter @NoArgsConstructor @AllArgsConstructor @Builder public class FxValuationRun { @Id @GeneratedValue(strategy=GenerationType.UUID) UUID id; String referenceNumber; LocalDate valuationDate; String currency; BigDecimal foreignAmount; BigDecimal previousRate; BigDecimal closingRate; BigDecimal valuationDifference; UUID accountingDocumentId; }