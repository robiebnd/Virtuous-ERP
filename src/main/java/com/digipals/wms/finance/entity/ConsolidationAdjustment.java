package com.digipals.wms.finance.entity; import jakarta.persistence.*; import lombok.*; import java.math.BigDecimal; import java.time.LocalDate; import java.util.UUID;
@Entity @Table(name="consolidation_adjustments") @Getter @Setter @NoArgsConstructor @AllArgsConstructor @Builder public class ConsolidationAdjustment { @Id @GeneratedValue(strategy=GenerationType.UUID) UUID id; @ManyToOne(fetch=FetchType.LAZY) @JoinColumn(name="group_id",nullable=false) ConsolidationGroup group; String unitCode; String accountCode; BigDecimal debit; BigDecimal credit; String reason; LocalDate periodStart; String status; }